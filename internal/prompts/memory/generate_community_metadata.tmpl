===任务===
给以下义相关的记忆实体生成代表性的「主题名称」与「主题摘要」. 只输出合法 JSON.

===记忆实体==
{{- range .Members }}
- {{ .Name }} {{ .Description }}
{{- end }}

===重要规则===
- 主题名称 `name` 使用中文, 并保持简洁 (不超过10字).
- 主题摘要使用一句话概括, 不超过60字.

===输出格式===
只输出如下 JSON:
```json
{
  "name": "主题名称",
  "summary": "主题摘要"
}
```

